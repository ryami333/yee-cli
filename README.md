# Installation (MacOS)

Add the following line to your `~/.zshrc`:

```
path+=$HOME/sites/yee-cli/bin
```

Reload `~/.zshrc`:

```zsh
source ~/.zshrc
```

You may need to run a "build" also:

```zsh
yarn build
```
